define(['exports', 'app', 'modernizr'], function (exports, _appEntry, _modernizr) {
   'use strict';

   describe('some string', function () {
      it('some strig', function () {
         except(1 + 2).toEqual(3);
      });
   });
});
