const config = {
    resize_tollerance: 10,
    cell_size: 40,
    default_width: 4,
    default_height: 4,
    default_operator: '+',
    operators: {
        '+':'sum',
        '-':'minus',
        '*':'multi'
    }
};

export default config